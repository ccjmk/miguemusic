info:
  name: New Song
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/songs
  body:
    type: json
    data: |-
      {
        "title": "Bohemian Rhapsody",
        "durationInSeconds": "360"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
