info:
  name: Song by ID
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/songs/:id
  params:
    - name: id
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
