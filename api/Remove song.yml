info:
  name: Remove song
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/songs/:id
  params:
    - name: id
      value: "10"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
